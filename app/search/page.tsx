import { SearchClient } from "./SearchClient"

export const metadata = {
  title: "Find a workspace | Nooc",
  description: "Search for calm workspaces by the hour near you.",
}

export default function SearchPage() {
  return <SearchClient />
}
